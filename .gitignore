node_modules
dist
.DS_Store
*.log

# Django/DRF port (django_api/) -- venv and bytecode, not the source
django_api/venv
django_api/**/__pycache__
django_api/**/*.pyc
django_api/db.sqlite3

# third-party essays (copyright) — regenerate locally with examples/lyn-alden/fetch-lyn.sh
examples/lyn-alden/*.txt

# personal learned voice examples (grows as you "Add to my voice"); seed.jsonl is committed
style/examples.jsonl

# local-only examples imported from other apps (may contain unposted drafts) + dismiss state
examples/zencub-*.txt
examples/_dismissed.json

# dev journal — local only, contains private notes
log.md
